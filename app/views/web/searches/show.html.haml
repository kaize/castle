= form_tag search_path, method: :get do
  = search_field_tag :q
  = submit_tag t('.search')

%ul
  - @results.each do |result|
    %li
      = render partial: result.searchable_type.underscore, object: result.searchable
