= simple_form_for @block, url: url, html: { class: 'default_class' } do |f|
  = f.input :state_event, as: :state_event
  = f.input :title
  = f.input :body, as: :ckeditor

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    = link_to t('.cancel'), admin_blocks_path, class: 'btn'
