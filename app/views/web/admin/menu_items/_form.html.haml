= simple_form_for @menu_item, url: url, html: { class: 'default_class' } do |f|
  = f.input :title

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
    = link_to t('.cancel'), admin_menu_items_path, class: 'btn'
