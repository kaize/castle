%h2= t('.instructors')
- @instructors.each do |i|
  .media.nav.nav-tabs.nav-stacked
    %a.pull-left
      .img-rounded.media-object= image_tag i.avatar.url
    .media-body
      .media-heading
        %h4= link_to i, instructor_path(i)

= paginate @instructors
